BEFORE RUNNING:
- install le express (npm install express)
- install le mongoose (npm install mongoose)
- run these 2 commands INSIDE the repo, their library files need to be incl. but they cant go on the github repo bc the file size is too large
- (if linux: remember to START mongodb before running LOLOLOL. sudo systemctl start mongod)

TO RUN:
- node server.js
